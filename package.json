{
  "name": "pulse-mesh",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "run": "bun src/main.ts",
    "build": "bun run typecheck",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "acceptance": "bun run scripts/acceptance.ts",
    "configure:local": "bun scripts/configure-env.ts",
    "action:local": "bun scripts/local-action.ts",
    "start": "bun src/main.ts"
  },
  "dependencies": {
    "cheerio": "^1.1.2",
    "rss-parser": "^3.13.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0"
  }
}
